export const environment = {
    production: true,
    apiUrl: 'placeholder-apiUrl', 
    auth: {
      clientId: 'placeholder-clientId', 
      authority: 'placeholder-authority', 
      redirectUri: 'placeholder-redirectUri', 
      knownAuthorities: ['placeholder-knownAuthorities'],
      scope: ['placeholder-scope']
    }
};
